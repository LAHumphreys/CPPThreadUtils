sudo: false

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
      - boost-latest
      
    packages:
      - cmake
      - cmake-data
      - g++-9
      - libboost-all-dev
      - valgrind
      - lcov

before_install:
  - export CXX="g++-9"
  - export CC="gcc-9"
  - g++-9 --version

install:
  - ./buildDeps.sh
  - pip install --user cpp-coveralls
  
script: 
  - ./buildFromDeps.sh
  - cd Build
  - make test
  - ctest -T Memcheck
  - cd ..
  - COVERALLS_FLAGS="--dump --verbose" ./deps/CMakeUtils/travis/doCoverage.sh
